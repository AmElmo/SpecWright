# Build Google Sign-In button component

**Issue ID**: ENG-007  
**Status**: pending  
**Priority**: high  
**Category**: frontend  
**Complexity Score**: 4/10  
**Estimated Hours**: 3h  
**Complexity Reasoning**: Standard component with Google OAuth library integration

> **Status Options**: `pending` (not started) | `in-review` (AI completed, needs human review) | `approved` (human verified)

## Description

Create reusable GoogleSignInButton component using @react-oauth/google library that initiates OAuth flow and handles the login process.

## Technical Details

Component should:
- Render Google-branded button
- Initiate OAuth flow on click
- Handle OAuth response
- Update auth context with user data
- Show loading state during auth
- Handle errors gracefully

```typescript
import { GoogleOAuthProvider, GoogleLogin } from '@react-oauth/google';

export const GoogleSignInButton = () => {
  const { login } = useAuth();
  
  const handleSuccess = async (credentialResponse) => {
    // Send credential to backend
    // Update auth context
  };
  
  return (
    <GoogleLogin
      onSuccess={handleSuccess}
      onError={() => console.error('Login Failed')}
    />
  );
};
```

## Dependencies

- **ENG-006**: AuthContext must exist to update login state

## Acceptance Criteria

- [ ] Component renders Google-branded button
- [ ] Click initiates OAuth flow
- [ ] Successful login updates auth state
- [ ] Failed login shows error message
- [ ] Loading state displayed during auth
- [ ] Component is reusable across pages
- [ ] Proper error handling and user feedback

## Test Strategy

### Automated Tests

- Component renders without crashing
- Click handler fires OAuth flow
- Success callback updates auth context
- Error callback shows error message
- Loading state displays correctly

### Manual Verification (Human-in-the-Loop)

1. Render button on login page
2. Click button and verify Google modal opens
3. Complete OAuth and verify login success
4. Deny permission and verify error handling
5. Test on different screen sizes
6. Verify button styling matches design

## Subtasks

1. Install @react-oauth/google package
2. Create GoogleSignInButton component
3. Implement success handler
4. Implement error handler
5. Add loading states
6. Style button to match design
7. Add component to login page
8. Test complete flow

---

**Notes**: Use official Google button for consistent branding. Consider one-tap sign-in for better UX on supported browsers.




